# Repository Metadata

## GitHub About description

> Transactional workspace isolation and exact-diff commit brokering for safer coding agents. Independent RFC and executable Python reference implementation.

## Longer directory or announcement description

> Agent Workspace Guard keeps coding agents out of the live workspace during task execution, then persists only an exact, signed, policy-approved filesystem result through a quarantine-first broker. The repository contains an independent RFC, threat model, security argument, production integration checklist, executable Python reference implementation, and destructive-action replay corpus.

## Homepage

Use the five-minute architecture summary:

```text
https://github.com/dmonsta86/agent-workspace-guard/blob/main/docs/ONE_PAGE.md
```

## Suggested topics

```text
ai-agents
coding-agents
agent-safety
sandbox
filesystem-security
secure-by-design
transactional-workspace
commit-broker
codex
python
```

## GitHub CLI command

```bash
gh repo edit dmonsta86/agent-workspace-guard \
  --description "Transactional workspace isolation and exact-diff commit brokering for safer coding agents. Independent RFC and executable Python reference implementation." \
  --homepage "https://github.com/dmonsta86/agent-workspace-guard/blob/main/docs/ONE_PAGE.md" \
  --add-topic ai-agents \
  --add-topic coding-agents \
  --add-topic agent-safety \
  --add-topic sandbox \
  --add-topic filesystem-security \
  --add-topic secure-by-design \
  --add-topic transactional-workspace \
  --add-topic commit-broker \
  --add-topic codex \
  --add-topic python
```
